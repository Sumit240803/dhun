// PUBLIC API of the realtime module.
// Responsibility: RTC token minting + provider (ZEGOCLOUD/Agora) abstraction
// Other modules import ONLY from this file — never from internal files.
export {};
