// PUBLIC API of the admin module.
// Responsibility: back-office API: KYC review, payouts, moderation, economy config
// Other modules import ONLY from this file — never from internal files.
export {};
